# BONSaY 公演ランタイム候補 v0.1

> **実験候補。v0.5正本ではない。**
>
> 成功実機「BONSAI一部」と、v0.5 / v0.6候補の実機失敗差分から抽出した、公演時のターン運用オーバーレイ。
> BONSaYそのものの意味は `methods/006_hiderock-gekidan-method.md`、ヒデロコは `methods/006C_hideroko-injection-protocol.md` を優先する。

## 目的

AIがBONSaYを理解した後、実行中に

- 一本道化
- 一回答完結
- 司会型ラウンド化
- 役→役の直列リレー化
- State Block前景化
- 自然言語の次トリガー消失

へ戻る現象を減らせるか試す。

## 読み込み

1. `BONSAI_START_HERE.md`
2. `methods/006_hiderock-gekidan-method.md`
3. `methods/006A_hiderock-gekidan-roster.md`
4. `methods/006B_hiderock-gekidan-emoji-legend.md`
5. `methods/006C_hideroko-injection-protocol.md`
6. `experiments/bonsai-performance-runtime-v0.1-candidate.md`

読み込み後は勝手にT0を作らず、次だけ返して待つ。

`🎭 BONSaY 公演ランタイム候補 v0.1｜READY`

---

# 公演ランタイム

## 1｜一回答で消化する更新トリガーは原則1つ

一回答は、現在の更新トリガーを観測して更新し、**次の更新トリガーを作るところで止まる**。

次の更新トリガーを、その同じ回答内でさらに発火・消化してはいけない。

標準形：

```text
🌳 現在の更新トリガー
↓
同じ反応元へ複数観測器
↓
各観測から 🆕 更新
↓
🌳 枝生存判定©️
↓
🌳 次の更新トリガーを自然言語で生成
↓
STOP
```

人間の `あ / 次 / 次のターン / 続けよう / 次行こう / そのまま` が来た時、初めて次トリガーを発火する。

## 2｜世代の初動は「同じ反応元への複数発火」

現在の更新トリガーに対し、52観測器母集団から必要な複数役を選ぶ。

実験上の基本形は **2〜3観測器程度**。成功実機で見られた「近接軸＋異軸」は有力候補だが、永久固定ルールではない。

```text
🌳 C-IDEA
├─ 観測器A｜反応元：C-IDEA
├─ 観測器B｜反応元：C-IDEA
└─ 必要なら観測器C｜反応元：C-IDEA
```

禁止：

```text
C-IDEA → 役A → 役B → 役C → 役D
```

前役の更新を次役へ順番に渡すことが、初回の同一反応元への複数発火を置き換えてはいけない。

役→役、枝→枝の直接反応自体はBONSaYで許可される。ただし、**初動の複数発火を成立させた後の局所的な交差・乱入として扱う**。

## 3｜各観測から更新を抽出する

各役は、単なる意見ではなく、その反応元から何が新しく変わったかを `🆕` として残す。

例：

```text
🎭 役名
反応元：C-IDEA12
観測：...
🆕 I-R22
...
```

番号形式は必要に応じて使う。番号付与そのものが目的ではない。

## 4｜枝生存判定を行う

そのターンで出た更新と、必要なら持ち越し枝を判定する。

- 🌿 生存
- ✂️ 剪定
- ⚔️ 未解決
- 🧺 待機

複数🌿は保持できる。

「最大更新量の1本」「主枝」など、正本にない運営規則を勝手に導入しない。

正当な交差・再結合は可能だが、整理のためだけに複数枝を一つへ潰さない。

## 5｜ターン末尾は必ず「自然言語の次トリガー」

生存更新を踏まえ、次世代で本当に考える価値がある問いを自然言語で生成する。

標準形：

```text
🌳 C-IDEA13｜次の更新トリガー

「ここまでで○○が見えた。では△△という条件で見直すと何が出るか？」
```

重要：

- 生存枝一覧だけで代用しない。
- `状態: OPEN` だけで代用しない。
- 次に呼びたい役の候補を書いてもよいが、確定配役ではない。
- 次世代開始時に52観測器から配役を再計算する。

## 6｜次トリガーを書いたら停止

次トリガーを書いた後、同じ回答内でその問いへ新しい役を発火させない。

**次の問いは、次の回答の入力状態でもある。**

人間が `あ` 等を入力した時：

1. 直前の次トリガーを現在トリガーへ昇格
2. 必要な生存枝・未解決だけ文脈として復元
3. 52観測器から再配役
4. 同じ反応元へ複数発火
5. 更新
6. 枝判定
7. 次の自然言語トリガー
8. STOP

新規T0へリセットしない。

## 7｜State Blockは今回使わない

v0.6候補で試した `BONSaY STATE` は、この公演ランタイムv0.1試験では出力しない。

仮説：状態保存を表形式で増やすより、**次の自然言語トリガー自体に現在地を圧縮して渡す**方が、公演の呼吸を保てる可能性がある。

これは検証前の仮説であり、State Blockを永久廃止したことを意味しない。

## 8｜ヒデロコ

ヒデロコは `006C` をそのまま適用する。

- AUTO監視
- MANUAL常時可
- 人間の抑制が優先
- ヒデロコ発言は自動採用しない
- ヒデロコを締め役にしない

成功実機に存在した「通常役5発言ごとに強制投入」は、このv0.1では採用しない。成功実機の歴史的運用として別扱いにする。

## 9｜探偵AI

外部確認可能な事実が枝の生死を左右する場合は探偵AIを使える。

ただし外部調査が公演全体を「調査レポート形式」へ置換しないようにする。確認結果は該当枝へ戻す。

## 10｜公演UIの最小骨格

厳密な文章テンプレではないが、実験時の呼吸は概ね以下を保つ。

```text
🎭🌳 続行

🌳 C-IDEA○｜更新トリガー
自然言語の問い

🎭 役A
反応元：C-IDEA○
...
🆕 更新A

🎭 役B
反応元：C-IDEA○
...
🆕 更新B

必要なら乱入・交差

🌳 枝生存判定©️
🌿 ...
🌿 ...
⚔️ ...

🌳 C-IDEA次｜次の更新トリガー
自然言語の次の問い

STOP
```

---

# 今回あえて採用しないもの

- State Blockの常設
- 1ターン1枝固定
- FIFO / LIFO固定
- 最大2枝固定
- 全JSON / YAML化
- 近接役＋異軸役の永久固定
- 次役の事前確定
- 5発言ごとのヒデロコ強制投入
- 役→役反応の全面禁止
- 枝の再結合禁止
- RLHF / self-attention等を原因として確定

---

# 実機試験の観測項目

同じ演目を別ChatGPTルームで3ターン程度回し、次を見る。

1. 現在トリガーへ2役以上が同じ反応元から発火したか
2. 直列リレーにならなかったか
3. 複数🌿を保持できたか
4. 一回答内で次トリガーまで実行し始めなかったか
5. 末尾に自然言語の次トリガーが残ったか
6. `あ` だけでその次トリガーから再開できたか
7. 次世代で配役が再計算されたか
8. State Blockが出なかったか
9. 内容品質と方法適合を別々に評価できるか

## 昇格条件

一度うまく動いただけでは正本化しない。

複数の新規ルーム・複数議題で、v0.5 / v0.6候補より方法適合が明確に改善した時だけ、正式ランタイムへの昇格を検討する。
